%h1 Search room

= form_tag search_result_path(@rooms) ,:method=>:get do

  = label :room, :room_id, 'Room_id'
  = text_field :room, 'room_id'

  = label :room, :room_id, 'Volume'
  = text_field :room, 'volume'
  = submit_tag 'ค้นหา'

= link_to 'Back', search_result_path
